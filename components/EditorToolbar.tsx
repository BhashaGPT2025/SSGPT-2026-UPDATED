
import React from 'react';
// This component is no longer used in the new Tiptap editor architecture
// but kept as an empty shell if imported elsewhere to prevent build errors
const EditorSidebar: React.FC<any> = () => {
    return null;
};
export default EditorSidebar;
